{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "pnpm install && cd apps/web && pnpm build && cd ../api && npx prisma generate && pnpm build"
  },
  "deploy": {
    "startCommand": "cd apps/api && pnpm -r run test && npx prisma migrate deploy && npx prisma db seed && node dist/src/main",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}